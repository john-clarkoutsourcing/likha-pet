import 'package:flutter/material.dart';

class AppColors {
  // ── Brand ─────────────────────────────────────────────────────────────────
  static const Color primary      = Color(0xFF6C3FA1); // deep violet
  static const Color secondary    = Color(0xFFE8A838); // warm gold
  static const Color accent       = Color(0xFF3FCFA1); // teal

  // ── Battle UI ─────────────────────────────────────────────────────────────
  static const Color hpGreen      = Color(0xFF4CAF50);
  static const Color hpYellow     = Color(0xFFFFEB3B);
  static const Color hpRed        = Color(0xFFF44336);
  static const Color energyBlue   = Color(0xFF2196F3);
  static const Color shieldGold   = Color(0xFFFFD700);
  static const Color poisonPurple = Color(0xFF9C27B0);
  static const Color stunYellow   = Color(0xFFFFEB3B);

  // ── Trait type colours ────────────────────────────────────────────────────
  static const Color offensive    = Color(0xFFE53935); // red
  static const Color defensive    = Color(0xFF1E88E5); // blue
  static const Color support      = Color(0xFF43A047); // green
  static const Color utility      = Color(0xFFFF8F00); // amber

  // ── Surfaces ──────────────────────────────────────────────────────────────
  static const Color bg           = Color(0xFF1A1035); // dark navy
  static const Color surface      = Color(0xFF2A1F4E); // card bg
  static const Color surfaceLight = Color(0xFF3A2F6E);
  static const Color divider      = Color(0xFF4A3F7E);

  // ── Text ──────────────────────────────────────────────────────────────────
  static const Color textPrimary  = Color(0xFFF5F0FF);
  static const Color textSecondary= Color(0xFFB0A8D0);
  static const Color textMuted    = Color(0xFF7068A0);

  // ── Faint / disabled ──────────────────────────────────────────────────────
  static const Color fainted      = Color(0xFF555555);
}
